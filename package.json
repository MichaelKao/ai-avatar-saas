{
  "name": "ai-avatar-saas",
  "version": "0.1.0",
  "private": true,
  "description": "AI Avatar SaaS — 智能數位分身會議助理",
  "scripts": {
    "test:api": "playwright test tests/api/",
    "test:e2e": "playwright test tests/e2e/",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
